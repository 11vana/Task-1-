namespace Task_1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            double princePerSmall = 25;
            double princePerLarge = 35;
            double taxRate = 0.06;
            Console.WriteLine("Islam carpet Cleaning Service");

            Console.WriteLine("Enter number os small carpets");
            int smallCarpets = Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Enter number os large carpets");
            int largeCarpets = Convert.ToInt32(Console.ReadLine());

            double cost = (smallCarpets * princePerSmall) + (largeCarpets * princePerLarge);
            double tax = (cost * taxRate);
            double totalEstimate = cost + tax;
            Console.WriteLine($"Number of small carpets: {smallCarpets}");
            Console.WriteLine($"Number of large carpets: {largeCarpets}");
            Console.WriteLine($"price per small carpets: ${princePerSmall}");
            Console.WriteLine($"price per large caepets: ${princePerLarge}");
            Console.WriteLine($"Cost : ${cost}");
            Console.WriteLine($"Tax : ${tax}");
            Console.WriteLine("====================================================");
            Console.WriteLine($"Total estimate : ${totalEstimate}");
            Console.WriteLine("This estimate is valid for 30 days");
        }
    }
}
